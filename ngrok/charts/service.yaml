apiVersion: v1
kind: Service
metadata:
  labels:
    app: ngrok-wernight
  name: ngrok-wernight-service
  namespace: default
spec:
  clusterIP: 10.43.244.184
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: ngrok-wernight
  sessionAffinity: None
  type: ClusterIP