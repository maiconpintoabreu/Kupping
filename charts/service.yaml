apiVersion: v1
kind: Service
metadata:
  labels:
    app: kupping-frontend
  name: kupping-frontend-service
  namespace: default
spec:
  clusterIP: 10.15.240.13
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: kupping-frontend
  sessionAffinity: None
  type: ClusterIP