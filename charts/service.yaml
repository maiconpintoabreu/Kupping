apiVersion: v1
kind: Service
metadata:
  labels:
    app: kupping-frontend
  name: kupping-frontend-service
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: kupping-frontend
  sessionAffinity: None
  type: ClusterIP